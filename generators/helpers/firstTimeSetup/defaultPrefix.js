const defaultPrefix = {
	description: `Enter the bot's default prefix.
		
		Default Prefix`
		.split("\t").join(""),       					// Remove tabs.
	default: "bot.",
	required: false,
};



module.exports = defaultPrefix;
