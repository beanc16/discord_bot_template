const homeServerInvite = {
	description: `Enter the a link to the bot's home server.
				(Displayed when a user runs the about command.)
		
		Home Server Invite`
		.split("\t").join(""),       					// Remove tabs.
	required: false,
};



module.exports = homeServerInvite;
